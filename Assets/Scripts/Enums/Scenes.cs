namespace Enums
{
    /// <summary>
    /// Used to avoid errors. This must be edited whenever a scene is renamed.
    /// </summary>
    public enum Scenes
    {
        MainMenu,
        SingleplayerGame,
        Preparation,
        
    }
}
